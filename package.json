{
  "name": "stasis",
  "version": "0.1.0",
  "description": "Synthetic physiology for the Pi coding agent: homeostatic internal state that causally modifies agent behavior",
  "license": "MIT",
  "type": "module",
  "private": true,
  "pi": {
    "extensions": [
      "./src/extension.ts"
    ]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "demo:sequence": "tsx scripts/demo-sequence.ts",
    "experiment": "tsx experiments/runner.ts",
    "analyze": "tsx experiments/analysis.ts"
  },
  "dependencies": {
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.84.2",
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@types/node": "^26.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
